const express = require('express');
const router = express.Router();


router.get('/:type', function(request, response){
    response.send(`${req.params.type} pizza! Good choice!`);

});




module.exports = router;